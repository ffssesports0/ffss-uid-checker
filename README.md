# FFSS UID CHECKER PROXY

This is the backend proxy for Free Fire UID Checker.
Used to bypass CORS and fetch UID info safely.

## Endpoint:
POST /ff

Body Example:
{
  "uid": "1234567890"
}